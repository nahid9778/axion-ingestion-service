apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingestion-ingress

spec:
  ingressClassName: nginx
  rules:
  - host: ingestion-service.hotfix.space
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: ingestion-service
            port: 
              number: 80
